package ru.whoy.webdb.entity.sub;

public enum Verdict {
    ERROR,      // запрет на действия, операция не учитывается
    CONFIRM,    // необходимо подтверждение
    NONE        // ни чего не делать
}
